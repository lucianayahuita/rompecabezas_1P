import 'package:flutter/material.dart';

class AppColors {
  // Púrpuras Principales
  static const Color primary = Color(0xFF6B21A8);       // Púrpura oscuro para botones y encabezados
  static const Color primaryDark = Color(0xFF3B0764);   // Púrpura profundo para el fondo dark
  static const Color primaryLight = Color(0xFF7E22CE);  // Púrpura medio para tarjetas de acción

  // Lilas de Fondo y Contenedores
  static const Color background = Color(0xFFF3E8FF);    // Lila claro principal de la app
  static const Color cardBackground = Color(0xFFE9D5FF);// Lila para tarjetas y elementos contenedores
  static const Color inputBackground = Color(0xFFF5F3FF);

  // Textos y Contraste
  static const Color textPrimary = Color(0xFF581C87);   // Texto principal en tono púrpura oscuro
  static const Color textSecondary = Color(0xFF7E22CE); // Texto secundario / subtítulos
  static const Color textLight = Colors.white;

  // Bordes y Sombra
  static const Color border = Color(0xFFD8B4FE);        // Bordes finos de los inputs y cards
  static const Color shadow = Color(0x1A4C1D95);
}